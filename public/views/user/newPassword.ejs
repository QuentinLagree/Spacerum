<%- include('../template/header') %>
<%- include('../componants/topbar') %>

<link rel="stylesheet" href="/assets/css/form.css">
<main>
    <% if (locals.flash && locals.flash.error) { %>
    <div class="error box" id="suc">
        <p><%= flash.error%></p>
        <div class="cross">
            <div class="layers"></div>
        </div>
    </div>
   <% } %> 
    <% if (locals.flash && locals.flash.success) { %>
    <div class="success box" id="suc">
        <p><%= flash.success %></p>
        <div class="cross">
            <div class="layers"></div>
        </div>
    </div>
   <% } %>
    <div class="container">
    <h2>Nouveau mot de passe</h2>
    <h3>Assure toi que le mot de passe soit différent de l'ancien.</h3>
        <form action="/password/recup/<%= email %>/<%= token %>" method="post">
            <div class="inputs">
                <%- include("../componants/password-eye-input") %>
                <input type="password" name="password-confirm" placeholder="Confirmer le nouveau mot de passe">
            </div>
            <input type="submit" value="Modifier votre mot de passe">
        </form>
    </div>
</main>
<%- include('../template/footer') %>